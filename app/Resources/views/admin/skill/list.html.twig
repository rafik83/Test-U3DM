{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref_design' %}
{% set activeMenu = 'ref_skill' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_skill_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.skill.add.title'|trans }}</a></div>
                    {{ 'admin.skill.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.skill.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.skill.list.column.id'|trans }}</th>
                            <th>{{ 'admin.skill.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for skill in refs %}
                            <tr{% if not skill.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ skill.id }}</td>
                                <td><a href="{{ path('admin_skill_edit', { 'id': skill.id }) }}">{{ skill.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_skill_edit', { 'id': skill.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}