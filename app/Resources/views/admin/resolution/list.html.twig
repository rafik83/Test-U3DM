{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref_design' %}
{% set activeMenu = 'ref_resolution' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_resolution_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.resolution.add.title'|trans }}</a></div>
                    {{ 'admin.resolution.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.resolution.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.resolution.list.column.id'|trans }}</th>
                            <th>{{ 'admin.resolution.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for resolution in refs %}
                            <tr{% if not resolution.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ resolution.id }}</td>
                                <td><a href="{{ path('admin_resolution_edit', { 'id': resolution.id }) }}">{{ resolution.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_resolution_edit', { 'id': resolution.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}