{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref' %}
{% set activeMenu = 'ref_finishing' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_finishing_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.finishing.add.title'|trans }}</a></div>
                    {{ 'admin.finishing.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.finishing.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.finishing.list.column.id'|trans }}</th>
                            <th>{{ 'admin.finishing.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for finishing in refs %}
                            <tr{% if not finishing.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ finishing.id }}</td>
                                <td><a href="{{ path('admin_finishing_edit', { 'id': finishing.id }) }}">{{ finishing.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_finishing_edit', { 'id': finishing.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}