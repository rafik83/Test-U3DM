{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref' %}
{% set activeMenu = 'ref_technology' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_technology_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.technology.add.title'|trans }}</a></div>
                    {{ 'admin.technology.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.technology.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.technology.list.column.id'|trans }}</th>
                            <th>{{ 'admin.technology.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for technology in refs %}
                            <tr{% if not technology.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ technology.id }}</td>
                                <td><a href="{{ path('admin_technology_edit', { 'id': technology.id }) }}">{{ technology.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_technology_edit', { 'id': technology.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}