{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref_design' %}
{% set activeMenu = 'ref_project_type' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_project_type_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.project_type.add.title'|trans }}</a></div>
                    {{ 'admin.project_type.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.project_type.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.project_type.list.column.id'|trans }}</th>
                            <th>{{ 'admin.project_type.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for project_type in refs %}
                            <tr{% if not project_type.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ project_type.id }}</td>
                                <td><a href="{{ path('admin_project_type_edit', { 'id': project_type.id }) }}">{{ project_type.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_project_type_edit', { 'id': project_type.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}