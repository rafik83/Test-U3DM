{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref_design' %}
{% set activeMenu = 'ref_technology_scanner' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_technology_scanner_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.technology_scanner.add.title'|trans }}</a></div>
                    {{ 'admin.technology_scanner.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.technology_scanner.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.technology_scanner.list.column.id'|trans }}</th>
                            <th>{{ 'admin.technology_scanner.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for technology_scanner in refs %}
                            <tr{% if not technology_scanner.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ technology_scanner.id }}</td>
                                <td><a href="{{ path('admin_technology_scanner_edit', { 'id': technology_scanner.id }) }}">{{ technology_scanner.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_technology_scanner_edit', { 'id': technology_scanner.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}