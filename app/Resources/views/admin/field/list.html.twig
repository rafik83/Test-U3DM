{% extends 'admin/layout.html.twig' %}

{% set activeTab = 'ref_design' %}
{% set activeMenu = 'ref_field' %}

{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default panel-table">
                <div class="panel-heading panel-heading-divider">
                    <div class="tools"><a href="{{ path('admin_field_add') }}"><span class="icon s7-magic-wand"></span> {{ 'admin.field.add.title'|trans }}</a></div>
                    {{ 'admin.field.list.title'|trans }}
                    <span class="panel-subtitle">{{ 'admin.field.list.subtitle'|trans }}</span>
                </div>
                <div class="panel-body">
                    <table id="table-ref-list" class="table table-striped table-hover">
                        <thead>
                        <tr>
                            <th>{{ 'admin.field.list.column.id'|trans }}</th>
                            <th>{{ 'admin.field.list.column.name'|trans }}</th>
                            <th class="actions"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for field in refs %}
                            <tr{% if not field.enabled %} class="highlight highlight-red"{% endif %}>
                                <td>{{ field.id }}</td>
                                <td><a href="{{ path('admin_field_edit', { 'id': field.id }) }}">{{ field.name }}</a></td>
                                <td class="actions"><a href="{{ path('admin_field_edit', { 'id': field.id }) }}" class="icon"><i class="s7-right-arrow"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}